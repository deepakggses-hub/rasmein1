# Application code is never web-servable. If a request reaches this file,
# the document root is misconfigured — deny rather than expose source.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
